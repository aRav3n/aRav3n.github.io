// Initiate global variables
const navButton = document.querySelector('#navIcon');
let navItem = {};
let navItems = [];

// Populate list of navItems
function 

// Create menu list with links and (text || icon)

// Display menu on click